repo: 'https://github.com/tai271828/charm-layer-sge-client.git'
includes:
  - 'layer:basic'
  - 'layer:apt'
  - 'interface:sge-cluster'
options:
  apt:
    packages:
      - gridengine-client
      - gridengine-exec
      - mpich
      - nfs-common

